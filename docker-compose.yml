services:
  backend:
    build: .
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/raw_data.db
    volumes:
      - boti_db:/app/data
      - ./raw_data:/app/raw_data
    restart: unless-stopped

volumes:
  boti_db:
